# HNP-BUILD-20260911

status: COMPLETE
editor_owner: root

Goal: rebuild the current saved Unity project as Web, without reimporting historical art or changing gameplay.
Inputs: current NakornpathomWorld scene, Unity 6000.6.0f1 and existing package versions.
Writes: guarded build entrypoint, `builds/rebuild-20260911-r1/web`, logs/test evidence and local server launcher.
Acceptance: successful build, local browser smoke/regression checks, verified served revision. Physical mobile NOT RUN.

Preflight: no active Unity process. Root exclusively owns Editor for this build. Existing references, models and prior builds are retained.

## Results

- Build PASS: `reports/world/rebuild-20260911-r1.log`; 0 errors, 357 warnings (including deprecated APIs and package shader warnings); 53,492,804 bytes uncompressed. No warning-clean claim.
- Browser PASS: `reports/world/world009-reference-20260911-084324/report.json` (movement, static idle, accelerated time, map, overhead camera, portrait/night).
- Shrine PASS: `reports/world/world010-shrine-20260911-084411/report.json` (actual input, intact photo aspect/unlit material, day/night and portrait screenshots).
- Chrome desktop 152.0.7977.83, software WebGL; physical mobile/performance NOT RUN.
- WASM SHA256: `7222c7d2919792cf35531f027501c720f3fb6d5ec48042e14437684863047369`. Same runtime hash as prior build is expected: this request rebuilds the saved project without gameplay edits.
- Local server: `http://localhost:8081`, target `builds/rebuild-20260911-r1/web`; launcher updated in `tools/serve-world003.ps1`. No public deployment.

Replay: open local URL, press Thai start, walk to the Chedi, drag camera, tap map, toggle night in the upper-left menu and rotate viewport. Use Ctrl+F5 if an earlier game page remains cached.
